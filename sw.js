/* ============================================================
   DukanList Service Worker
   Strategy:
     - install : precache key static assets
     - fetch   : network-first, fallback to cache
     - skip    : /admin/*, /panel/*, supabase API, POSTs, non-GET
   ============================================================ */
const VERSION    = 'dukan-v2.9.61';  // POST-REGISTRATION ONBOARDING FIX — User feedback: after registering, owners don't know they need to verify email; after clicking email link, no celebration page (just dumped at login). Two-part fix: (1) New panel/email-verified.html — beautiful celebration page that Supabase emailRedirectTo lands users on. Detects fresh session, looks up linked business, customizes copy based on status (pending vs active), shows 3-step timeline (Registered → Email Verified → Activation), fires confetti animation, shows 'what you unlocked' grid, auto-redirects to dashboard in 5s (cancellable). For pending listings, surfaces WhatsApp activation link with pre-filled message. (2) Redesigned register.html success card — added prominent 3-step visual progress at top (step 2 of 3 highlighted with orange pulse animation), promoted email verification to PRIMARY card with bold border/colored bg/clear copy, demoted WhatsApp to SECONDARY card with calmer styling. Updated emailRedirectTo in register.html + panel/claim-account.html to point to new email-verified page. Net effect: clear cognitive sequence (filled → verify email NOW → wait for activation) replaces previous mixed-signal flow where WhatsApp button competed with email task.