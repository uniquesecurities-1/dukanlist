/* ============================================================
   DukanList Service Worker
   Strategy:
     - install : precache key static assets
     - fetch   : network-first, fallback to cache
     - skip    : /admin/*, /panel/*, supabase API, POSTs, non-GET
   ============================================================ */
const VERSION    = 'dukan-v2.9.63';  // ADMIN UI 2026 REFRESH — User found admin look 2002-boring. Pure CSS-only modernization with ZERO HTML/JS/DB changes. Completely rewrote assets/css/admin-premium.css (~700 lines) with 2026 design system 'Saffron Quartz': warm off-white surfaces (#FAFAF7), deep ink type (#0F1729), refined saffron accents (#FF6B1A → #EA580C gradients), soft layered shadows (4-tier), 14-22px border radii, Plus Jakarta Sans typography with tabular numerals, micro-interactions on hover (lift + shadow), dark topbar (#0F1116 → #181B22) with saffron accent line. Redesigned: topbar (dark gradient + accent line), KPI cards (left-edge color stripe, hover lift), shop list (white cards, hover saffron border + transform), filter chips (pill shape, dark active state), buttons (primary saffron, danger red, good green, wa green — all with proper shadows), inputs (saffron focus ring), admin cards (hover lift, role-specific gradient pills), modals (frosted backdrop, soft border), tables (zebra hover), live-dot pulse animation, custom scrollbar, mobile responsive breakpoints, focus-visible orange rings. All 25/25 admin pages now load this stylesheet (added to health.html, incomplete-shops.html, spotlight.html — the 3 that previously didn't). Inspired by Linear/Stripe Dashboard/Vercel/Cal.com modern indie SaaS aesthetic. Zero risk: all class names, IDs, JS handlers, DOM structure, RPCs untouched — only visual properties overridden via !important. To revert: delete the CSS file.